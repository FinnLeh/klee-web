[project]
name = "klee-web"
version = "0.1.0"
description = "Browser-accessible KLEE symbolic execution."
readme = "README.md"
requires-python = ">=3.12"
license = "NCSA"
authors = [
    { name = "Software Reliability Group, Imperial College London" },
]
dependencies = [
    "celery>=5.6.3",
    "fastapi>=0.139.2",
    "pydantic>=2.0",
    "pydantic-settings>=2.14.2",
    "redis>=8.0.1",
    "uvicorn[standard]>=0.30",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "httpx>=0.27",
    "ruff>=0.15.22",
    "fakeredis>=2.36.2",
    "mypy>=2.3.0",
]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/klee_web"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]

[tool.mypy]
python_version = "3.12"
mypy_path = "src"
files = ["src", "tests"]
explicit_package_bases = true
namespace_packages = true

[[tool.mypy.overrides]]
module = "celery.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "--strict-markers"
markers = [
    "requires_docker: integration test that needs the Docker CLI and the KLEE runner image",
]
